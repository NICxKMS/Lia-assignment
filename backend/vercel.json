{
	"_comment": "This file is for Vercel deployment (legacy/alternative). Active deployment uses Render — see root render.yaml.",
	"$schema": "https://openapi.vercel.sh/vercel.json",
	"version": 2,
	"builds": [
		{
			"src": "api/index.py",
			"use": "@vercel/python"
		}
	],
	"routes": [
		{
			"src": "/(.*)",
			"dest": "api/index.py"
		}
	],
	"env": {
		"PYTHONUNBUFFERED": "1"
	}
}
